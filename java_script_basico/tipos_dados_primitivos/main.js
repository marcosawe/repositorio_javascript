// Dados Primitivos: Strings, Number, undefined, Null, Boolean, Symbol.
// Strings -> Texto
const nome = "Marcos";
const nome1 = 'Marcos';
const nome2 = `Marcos`;

//Number -> Retorna um número
const numero1 = 10;
const numero2 = 10.34;

//Undefined -> Não aponta para nenhum local na memória
let algumNome;

//Null -> Também não aponta para nenhum local na memória, porém de forma proposital
const arquivo = null; 

//Boolean -> Retorna verdadeiro ou falso (Lógico)
const Boolean = true || false;

//Typeof -> mostra o valor representado da variável
console.log(typeof(numero1), numero1)
