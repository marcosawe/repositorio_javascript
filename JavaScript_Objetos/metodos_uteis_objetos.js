/*
Object.values // Retorna os valores das chaves de um Objeto;
Object.entries // Retorna em um array a chave e o valor de um Objeto;
Object.getOwnPropertyDescriptor(o, 'prop') // Descreve as propriedades impostas sobre o objeto;
Object.assign(des, any)
... (spread) // Copia os valores contidos em um Objeto; 
// Já vimos
Object.keys (retorna as chaves)
Object.freeze (congela o objeto)
Object.defineProperties (define várias propriedades)
Object.defineProperty (define uma propriedade)
*/