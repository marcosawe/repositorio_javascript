/*IIFE -> Immediatly invoked function expression */
// Utilizada para auto envocar uma função automaticamente, não afetando o escopo global.

(function(){
    const nome = 'Marcos'
    console.log(nome);
})();

const nome = 'Fernando'